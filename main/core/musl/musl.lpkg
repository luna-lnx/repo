# this package is an example. don't actually use it, TOML format

name = "musl"
description = "musl libc. nothing else to add."
tag = "1.2.4"
tarball = "https://git.musl-libc.org/cgit/musl/snapshot/musl-%s.tar.gz"
# TODO: add other formats
dependencies = [
    "make"
]
build = [
    "$SHELL -c ./configure",
    "make"
]
install = [
    "make install"
]
uninstall = [
    "make uninstall"
]