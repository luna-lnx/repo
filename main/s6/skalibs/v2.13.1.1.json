{
    "git": "https://github.com/skarnet/skalibs -b v2.13.1.1",
    "build": [
        "./configure --prefix=/usr/",
        "make"
    ],
    "install": [
        "make install"
    ],
    "uninstall": [
        "rm -rf /usr/lib/skalibs",
        "rm -rf /usr/include/skalibs",
        "rm -f /usr/lib/libskarnet.*"
    ]
}